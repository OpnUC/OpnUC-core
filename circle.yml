machine:
  environment:
    APP_ENV: testing
    APP_DEBUG: true
    DB_HOST: 127.0.0.1
    DB_DATABASE: circle_test
    DB_USERNAME: ubuntu
    DB_PASSWORD:
  php:
    version: 5.6.30

database:
  override:
    - php artisan migrate

test:
  override:
    - vendor/bin/phpunit -d memory_limit=512M